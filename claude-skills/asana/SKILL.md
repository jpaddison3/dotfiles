---
name: asana
description: Interact with Asana — tasks, My Tasks, projects, sections, tags, users, workspaces. Use whenever JP wants to read or modify Asana data (look up or edit a task, list My Tasks, comment, find a project, etc.).
---

# asana

Use the `dharma` CLI — an agent-friendly wrapper over the Asana API.

Run `dharma -h` for the top-level commands, then `dharma <command> --help` to drill in. `dharma api` makes raw Asana API requests for anything the subcommands don't cover.
